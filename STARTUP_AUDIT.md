# MindForge product and launch audit

Audited: 2026-09-07; live verification updated 2026-09-08. Scope: positioning, acquisition, onboarding, identity,
core learning journey, trust, accessibility, performance, data, reliability,
operations, testing, and production deployment.

## Executive verdict

MindForge is a coherent, credible private AI study product with a complete code
path from account creation through source upload, grounded conversation, study
tools, export, and account deletion. Clerk is now the authoritative identity
provider, while MindForge keeps a small internal user record for data ownership
and preferences. Existing email-matched users are linked only through a verified
Clerk primary address.

The application is now **development-beta ready end to end** at the canonical
Vercel URL. Supabase PostgreSQL, Upstash Redis, Clerk development authentication,
and Gemini free-tier inference are configured and exercised in production. The
exact user-supplied PDF completed upload, extraction, session creation, grounded
chat, structured study-tool generation, New Session, reload persistence, and
Library search. `/api/health` returns HTTP 200 with database and configuration
both `ok`.

It is not yet an unrestricted public-production launch. Clerk is intentionally
still using development keys, the Supabase restore procedure and human alert
delivery have not been proven, and GitHub's local-browser job cannot initialize
Clerk until repository secrets are added. Those are operator-release controls,
not blockers for the development-mode review the owner approved.

| Area | Verdict | Evidence and consequence |
| --- | --- | --- |
| Positioning | Pass | Landing, About, and feature copy consistently promise private, source-grounded study; unsupported pricing, collaboration, local-processing, and research claims are absent. |
| Acquisition | Pass for an early product | Public landing, feature explanation, GitHub, legal, security, and support routes are reachable without an account. No analytics or funnel instrumentation exists, so conversion cannot yet be measured. |
| Onboarding and identity | Operational in Clerk development mode | Clerk-hosted sign-up/sign-in, verified-email account linking, protected pages/APIs, user controls, sign-out, profile sync, and dual-system account deletion are implemented. Production-domain Clerk configuration remains a launch requirement. |
| Core study journey | Pass in live development mode | The signed-in production journey now passes with the exact PDF: upload/extraction, persisted session, grounded Gemini answer, structured summary, New Session, reload, and Library search. Professor–student tutoring supports a saved student-selected explanation language, one-click re-explanation, and matching browser speech locale. Read-aloud is a prominent labeled control above every professor explanation. Enter submits the chat composer, Shift+Enter preserves multiline input, and IME composition is protected. A layered study-only boundary rejects utility requests before persistence while preserving educational questions about the same subjects. Study-tool results remain visible in their originating tab and AI stream failures no longer create blank answers. |
| Data ownership and privacy | Pass for bounded volume, with disclosed free-tier tradeoff | Every material/session query is scoped through the internal user ID; downloads are private; deletes cascade; uploads are limited to 10 MB per file and five files per batch. Source bytes remain in PostgreSQL, which is unsuitable for unbounded scale. Relevant extracted material is sent to Gemini's free tier, where Google may use it to improve products; this is disclosed before submission and in Privacy. |
| Security | Strong baseline; operator controls pending | Clerk server auth is repeated at resource boundaries, mutations enforce same-origin, CSP is generated by Clerk middleware, abuse identities use a separate HMAC secret, Upstash-backed distributed limits fail closed, speech stays in the browser, and audit events are persisted. Clerk production keys/domain, rotation evidence, and alert delivery remain operator work. |
| Accessibility and responsive UX | Partial | The upload accessible-name and dark-theme brand contrast defects found in browser testing are fixed. Post-deploy Lighthouse accessibility is 100; automated axe, screen-reader, keyboard-only, 320 px, and 200% zoom sign-off remain absent. |
| Performance | Partial | The production build succeeds; shared first-load JavaScript is about 102 kB, public home is about 194 kB, and the heaviest workspace route is about 212 kB. Post-deploy mobile Lighthouse measured performance 92, FCP 1.8 s, LCP 3.2 s, and TTI 3.4 s. OCR and document processing remain synchronous. |
| Reliability | Runtime dependencies verified; scale controls pending | Public readiness distinguishes database and configuration failures, Upstash answers `PONG`, Gemini errors are surfaced to the client, server errors are structured, retention is scheduled, and the canonical health endpoint is green. No queue, retry ledger, provider fallback, or multi-region data plan exists. |
| Operations | Development deployment verified; public-launch evidence pending | CI, additive migration, release/rollback, restore-drill instructions, retention, incident response, and an interactive launch wizard are present. Supabase is deployed in `us-east-1` with pooled runtime and direct migration connections. A timed restore, named on-call owner, Clerk production domain, and alert-delivery proof remain missing. |
| Automated quality | Local pass; GitHub browser gate awaiting Clerk secrets | 25 behavioral unit tests, TypeScript, lint, production build, dependency audit, and five public browser checks pass locally. GitHub Actions reaches Playwright after its install, audit, migration, unit, type, lint, and build steps pass, but the local Playwright server cannot initialize Clerk because repository Clerk secrets are absent. The separate live authenticated audit passes manually. |

## Live Vercel audit

Production was exercised at `https://mind-forge-ashy.vercel.app` across
2026-09-07 and 2026-09-08 using a real Clerk development-mode session, the exact
file supplied by the user, network/console inspection, Vercel runtime logs,
route probes, and mobile Lighthouse.

### Final end-to-end production verification — 2026-09-08

- Deployment `dpl_GY3pehe1wLUBNQVqiw8XZS1knT5z` promoted read-aloud into a
  large labeled professor-message control and added client, server, model-prompt,
  and ambiguous-request classifier scope enforcement. Live weather prompts were
  rejected on both New Study Session and the existing chat without creating a
  session or seventh message. The Hindi read-aloud control entered active
  playback, stopped cleanly, and English preference persisted after reload.
- Deployment `dpl_8vM3ZdPnv9NXTHEvAM1xvgUDMpSg` added the multilingual
  Professor MindForge experience and reached `READY` on the canonical alias.
  A signed-in student selected Hindi, received a new conversational Hindi
  explanation grounded in Material 1, started Hindi read-aloud playback, then
  restored English. Reload preserved both the six-message conversation and the
  saved English preference. English, Hindi, Spanish (Latin America), Latin,
  French, German, Portuguese, Mandarin Chinese, Japanese, and Korean are
  available; actual voice inventory remains browser/device dependent.
- Deployment `dpl_4PWfjZCaprDE1aTyFjDJSiqJQodv` reached `READY` and owns the
  canonical aliases at that checkpoint. `/api/health` returns HTTP 200 with
  `database: ok` and `configuration: ok`.
- The exact `/Users/shubhamkumar/Downloads/ML GFG.pdf` file uploaded through the
  production file chooser, reached 100%, extracted successfully, and created
  persisted session `cmtt98etd0007sorhvihb9hil` with one material.
- Gemini `gemini-2.5-flash-lite` returned provider HTTP 404 for this new auth-key
  project. The failure was visible in Vercel logs and previously left a blank
  client bubble. The configured/default model is now the current free-tier
  `gemini-3.5-flash-lite`; the same session then returned a coherent answer
  grounded in Material 1.
- Streaming now reassembles SSE frames split across network chunks, propagates
  provider error events, removes failed optimistic bubbles, and refuses to
  persist an empty assistant message. Fragmented-frame and provider-error
  regression tests both pass.
- Generate Summary returned structured JSON with a title, summary, key points,
  and definitions derived from the PDF. A UI defect that hid results under Chat
  was reproduced and fixed; the result and loading state now appear directly in
  Study Tools.
- New Session navigated cleanly to `/workspace`, showed online study services,
  and preserved the completed session in the sidebar. Reload restored one
  material and four persisted messages. Library displayed one session and
  matched it for `machine learning` search.
- Browser inspection found no application console errors. The only warnings are
  Clerk's expected development-key notices. Browser speech is unavailable in
  the Codex in-app browser runtime, so its actual audio output remains a
  real-browser compatibility check; the server-paid TTS dependency has been
  removed and unsupported speech fails as a no-op.
- The production dependency audit is clean after pinning patched `sharp` 0.35.4
  while preserving the PostCSS 8.5.18+ override. Public Playwright passed all
  five tests against the canonical Vercel URL in 6.8 seconds.

### Initial incident reconstruction — 2026-09-07

The following findings describe the service-offline deployment that preceded
the final verification above. They are retained as root-cause and regression
evidence, not as the current production state.

- Landing, About, Privacy, Terms, Security, Support, sign-in, and sign-up returned
  HTTP 200. Anonymous protected API access returns JSON HTTP 401.
- `/Users/shubhamkumar/Downloads/ML GFG.pdf` is a valid 1,952,151-byte PDF 1.4
  document. `pdf-parse` extracted nine pages and 10,625 non-whitespace text
  characters in 80 ms. The file is not corrupt and is not the root cause.
- The exact PDF was selected through the signed-in production file chooser and
  displayed as `ML GFG.pdf` (1906.4 KB). `Analyze & Start Session` then failed;
  the browser reported `Failed to fetch`, and Vercel tied the upload request to
  Prisma initialization with an empty `DATABASE_URL`.
- The earlier New Session failure also reproduced deterministically. The client
  generated a random UUID and navigated to it without creating a session; the
  destination then displayed `Failed to load session: Request could not be
  completed`. A later patch stopped fabricating the route, but clicking New
  Session while already on `/workspace` still had no visible result.
- Vercel tied the failing request to `PrismaClientInitializationError` because
  production `DATABASE_URL` resolved to an empty string. Readiness therefore
  correctly returned HTTP 503 with database `unavailable` and configuration
  `invalid`.
- The workspace sidebar was also rendering three hard-coded 2024 demo sessions,
  all of which followed the same broken lookup path.
- This pass makes New Session reset the current draft, remove selected files,
  focus the topic input, and announce that a new session is ready. It also
  restores the sidebar collapse control, replaces demo rows with the
  authenticated `/api/sessions` result, and shows an explicit service-offline
  state. Analyze is disabled until `/api/health` is ready, so selected files
  remain local during an outage.
- Anonymous `/workspace`, `/library`, and `/settings` requests redirected to an
  unimplemented `/sign-in` route (HTTP 404). Middleware now redirects to the
  implemented `/auth/signin` route, with a Playwright regression check.
- The file input had a visual label but no programmatic association, which
  prevented label-driven assistive technology and browser automation from
  locating it. The label and input now share `htmlFor`/`id`.
- Study tools, export, and speech requests placed `credentials: "include"`
  inside the HTTP headers object. These three requests now use the Fetch API's
  top-level credentials option.
- The manifest advertised nonexistent PNG icons and the deployed favicon was a
  404. Metadata and the manifest now use the existing SVG asset.
- Session creation, materials, library data, AI responses, and study tools were
  initially blocked because PostgreSQL, OpenAI, and Upstash had non-working
  production values. The staged remediation is independently recorded below.

### Measured live quality before this deployment

- Mobile Lighthouse: performance 85, accessibility 95, best practices 73, SEO
  100; FCP 2.1 s, LCP 4.0 s, speed index 2.4 s, TTI 4.7 s.
- Lighthouse flagged the missing favicon/network 404, third-party Clerk
  development cookies, and insufficient dark-theme brand contrast. The first
  and third defects are fixed in this pass; scores must be remeasured after the
  deployment rather than inferred.
- Security headers observed in production include CSP, HSTS, `nosniff`,
  Referrer-Policy, Permissions-Policy, `X-Frame-Options: DENY`, and
  `Cross-Origin-Opener-Policy: same-origin`. Clerk's generated development CSP
  remains intentionally broad and should be reassessed with production Clerk.

### Earlier service-offline checkpoint — 2026-09-07

- Production deployment `dpl_EX5MKjFYU4FZDWHTR35dAHB8B2Zg` reached `READY`
  and was aliased to the canonical URL.
- In a signed-in browser, the exact PDF attached successfully. Analyze remained
  disabled with an explicit service-outage message. New Session removed the PDF,
  cleared the prompt, focused the prompt input, and announced the reset. The
  sidebar collapsed and expanded, and no application console errors were logged.
- `/favicon.svg` and `/site.webmanifest` returned HTTP 200. Anonymous protected
  pages return HTTP 307 to `/auth/signin`; protected APIs return JSON HTTP 401.
  `/api/health` remained HTTP 503 with database `unavailable` and configuration
  `invalid`.
- Post-deploy mobile Lighthouse: performance 92, accessibility 100, best
  practices 77, SEO 100; FCP 1.8 s, LCP 3.2 s, speed index 1.8 s, TTI 3.4 s,
  total blocking time 10 ms, and CLS 0.002.
- Remaining Lighthouse penalties are principally the Clerk development-instance
  handshake redirects/third-party cookies, 262 KiB of unused JavaScript, and
  document latency. The Clerk-related measurements should be reassessed after a
  production custom domain is configured; bundle and latency work remain P1.
- The automated Clerk-authenticated production run passed token setup, sign-in,
  exact-file attachment, and New Session reset. It then stopped at the explicit
  health assertion in 10.1 seconds, as intended while services are unavailable.

### Supabase production verification

- A free Supabase project was created in East US (`us-east-1`) to match the
  Vercel function region. The project Data API remains disabled because this
  application accesses PostgreSQL only through Prisma.
- Runtime traffic uses Supabase's transaction pooler on port 6543 with
  `pgbouncer=true` and a one-connection-per-function ceiling. Prisma migrations
  use the session pooler on port 5432 through a separate `DIRECT_URL`.
- A dedicated `mindforge_app` login has only connect access to the database and
  usage/create access on the public schema. The generated credential was sent
  directly to Supabase and Vercel and was not written to the repository.
- `prisma migrate deploy` connected to the production session pooler and
  successfully applied `202609070001_initial`,
  `202609070002_operational_audit`, `202609070003_session_revocation`, and
  `202609070004_clerk_identity`.
- Production deployment `dpl_ACcWCUS6Ny2MSKDe7iBhhYMdYgHE` reached `READY` and
  was aliased to `https://mind-forge-ashy.vercel.app`.
- At this checkpoint, `/api/health` reported database `ok`; its HTTP 503 was
  solely the remaining configuration gate. A fresh signed-in workspace request
  to `/api/sessions` returned HTTP 200 in Vercel runtime logs, and the sidebar rendered the
  truthful empty state instead of the storage-offline error.

## Clerk migration review

1. `clerkMiddleware()` covers every dynamic/API request and uses Clerk's current
   automatic CSP support, extended with `frame-ancestors 'none'`, `base-uri`, and
   `object-src` restrictions.
2. Server handlers do not trust client hooks. `requireAppUser()` calls Clerk's
   server `auth()` and resolves the external user to the internal ownership ID.
3. First-use linking requires a verified primary email, normalizes it, and uses
   the unique email constraint so existing study data is not orphaned.
4. Profile names are synchronized to Clerk. Account deletion removes the Clerk
   identity before cascading MindForge data deletion; a database failure after
   identity deletion leaves inaccessible data for operator cleanup rather than
   leaving an active account with undeleted private data.
5. The migration adds a nullable unique `clerkId`. Legacy credential columns and
   tables remain temporarily for rollback safety; no running code imports or
   exposes the old NextAuth, password, OTP, reset-token, OAuth, or SMTP flows.
6. Browser E2E now uses Clerk's current project-based Playwright setup with a
   setup-project dependency. This follows Clerk's documented pattern and avoids
   the deprecated function-style global setup. A disposable test user remains
   an operator requirement.

## Highest-priority launch work

### P0 — required before inviting users

1. Create/configure the Clerk production instance, require verified email,
   restrict authorized parties, add the production domain, and install its
   publishable/secret keys in Vercel.
2. Confirm Supabase backup policy and complete a timed restore drill before
   storing user data beyond launch testing.
3. Keep the restricted Gemini auth key and Upstash credentials under a documented
   rotation policy; never reuse Clerk, rate-limit, or retention secrets.
4. Add an automated Clerk-authenticated staging run covering upload, AI response,
   study tools, library visibility, deletion, and sign-out. The same journey has
   been completed manually in production development mode.
5. Approve Privacy/Terms, assign incident
   ownership, and prove uptime/error/retention alerts reach a human.
6. Add `CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` to GitHub Actions secrets
   so the public browser gate can initialize Clerk; then rerun the failed
   workflow and require a green result before release.

### P1 — first post-launch iteration

1. Add privacy-conscious activation analytics for landing CTA, verified signup,
   first upload, first successful answer, study-tool generation, and return use.
2. Add automated axe checks plus keyboard-only, screen-reader, 320 px mobile, and
   200% zoom acceptance tests for Clerk and workspace flows.
3. Move OCR, document extraction, and long AI generations behind durable jobs
   with idempotency keys, progress, retry/cancel behavior, and cost ceilings.
4. Move uploaded bytes to private object storage with short-lived signed access
   before increasing limits or usage volume.
5. Add deletion reconciliation or a provider webhook/outbox so rare partial
   failures can be detected and completed automatically.

### P2 — scale and differentiation

1. Build source-level citations from generated answers back to exact document
   passages; this is the clearest trust and differentiation opportunity.
2. Add spaced-repetition scheduling and measurable mastery loops instead of only
   generating static flashcards and quizzes.
3. Add per-user AI budgets, cost telemetry, latency/error service objectives,
   and model fallback policies before broad acquisition.
4. Validate demand before adding teams, collaboration, billing, or native apps;
   none should be advertised until implemented and measured.

## Evidence captured in this pass

- `vitest`: 15 files, 50 behavioral tests passing.
- TypeScript: no errors after regenerating current Prisma and Next route types.
- ESLint: clean, with only the repository's known legacy-config deprecation warning.
- Next.js production build: successful across 28 page/API routes and middleware.
- Launch wizard: `bash -n` clean; `shellcheck` is unavailable on this host.
- Playwright public suite: five checks passing, including all public/legal
  routes, product-branded Clerk sign-in, machine-readable readiness, anonymous
  API rejection, and the protected-page sign-in destination.
- Production dependency audit: no known vulnerabilities at high severity or
  above.
- Exact PDF: nine pages and 10,625 extracted characters; live upload, extraction,
  persisted session creation, grounded chat, and summary generation all passed.
- GitHub Actions Quality #17 completed with the same isolated failure at
  `pnpm test:e2e:public`; its install, audit, migration, unit, typecheck, lint,
  and build steps passed. A blank-key local reproduction confirms Clerk cannot
  initialize the Playwright server without repository Clerk secrets. The
  authenticated staging job is manual-only and was skipped.
- Supabase production evidence includes all four applied migrations, database
  readiness `ok`, and an authenticated session-list request returning HTTP 200.
  Upstash returned `PONG`; Gemini produced both grounded prose and a structured
  study-tool result. Neither result is inferred from compilation.

## Audit method

The pass used Matt Pocock-style implementation discipline, behavioral tests,
deep-module seams, and product-level truth checking. Kun Chen's no-mistakes
requirements supplied the validation, documentation, and push-safety standard.
The `no-mistakes` executable is not installed on this host, and its PR workflow
conflicts with the explicit direct-to-`main`, no-PR instruction, so equivalent
local gates are run manually and their limits are reported rather than hidden.
